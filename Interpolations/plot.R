results = read.csv("lagrange_dump.csv")
results2 = read.csv("newton_dump.csv")
results3 = read.csv("gsl_dump.csv")
points = read.csv("xy_dump.csv")
library("ggplot2")
ggplot(points,aes(x=x, y=y, colour="points")) + geom_point()
last_plot() + geom_line(data=results, aes(x=x, y=f, colour="lagrange"))
last_plot() + geom_line(data=results2, aes(x=x, y=f, colour="newton"))
last_plot() + geom_line(data=results3, aes(x=x, y=f, colour="gsl"))